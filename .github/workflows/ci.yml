name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up shell
        run: chmod +x scripts/verify_repo.sh || true
      - name: Verify repository structure
        run: |
          ./scripts/verify_repo.sh
