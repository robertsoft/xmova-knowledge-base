apiVersion: xmova/v1
kind: Pipeline
metadata:
  name: 417123201--417123201_src_app_screens--ResumoApontamentoDetail
  labels:
  - recordDetail fields=data,nomeEncarregado,nomeFuncionario,tipoOperacao,perda,atividade,frente,folhaTarefa
    labels
pipeline:
  tasks:
  - id: get__lastApontamento
    name: get__lastApontamento
    run:
      type: resource_query
      resource: _lastApontamento
  - id: query__lastApontamento
    name: query__lastApontamento
    run:
      type: sql
      sql: select * from ResumoApontamento order by nomeEncarregado, data
  - id: recorddetail_fields_3
    name: recorddetail_fields
    run:
      type: emit
    outputs:
    - name: data
    - name: nomeEncarregado
    - name: nomeFuncionario
    - name: tipoOperacao
    - name: perda
    - name: atividade
    - name: frente
    - name: folhaTarefa
hooks:
  beforeInit:
  - _lastApontamento: select * from ResumoApontamento order by nomeEncarregado, data
  - _lastApontamento: 'null'
resources:
- name: _lastApontamento
  type: sql
  sql: select * from ResumoApontamento order by nomeEncarregado, data
secrets: {}
